# 기여·작업 흐름

먼저 [AGENTS.md](AGENTS.md), [문서 지도](docs/INDEX.md), [현재 상태](harness/state.json)를 읽습니다. 이 저장소는 아직 제품 구현 전입니다. 문서 하네스 검사기는 제품 테스트가 아닙니다.

## 작업 선택

작업 원장의 todo 중 의존 작업이 done인 항목을 선택합니다. 기본 동시 작업 수는 하나입니다. TASK-001부터 시작하며 프로젝트 전체를 한 번에 구현하려 하지 않습니다. 범위·수용 조건·관련 F/E-ID와 수정 파일을 작업 기록에 적습니다.

브랜치 이름은 feat/TASK-001-short-name, fix/TASK-xxx-short-name, docs/short-name 형태를 권장합니다. 원격 HEAD와 로컬 변경을 확인하고 기존 작업을 덮지 않습니다. 새 데이터나 외부 API가 필요하면 출처·동의·비용을 먼저 확인합니다.

## 구현과 검토

작은 합성 실패 재현 → 최소 구현 → 관련 평가 → 전체 회귀 → 안전/출처/버전 검토 순서입니다. 새 범주에는 정상·오류·충돌·unknown 사례가 필요합니다. 문서 계약을 바꾸면 관련 원장·평가·결정 기록도 갱신합니다.

커밋은 목적별로 작게 만들고 제품 코드, fixture, 문서, 결과 근거를 구별합니다. 실제 게임 콘텐츠·개인정보를 커밋하지 않습니다. 원장만 done으로 바꾸거나 테스트를 생략해 완료 처리하지 않습니다.

PR에는 목적, TASK/F/E-ID, 변경 범위, 실제 검증 명령과 결과, 미실행 이유, breaking change, 위험·복구, 문서 갱신을 포함합니다. [검토 템플릿](harness/templates/review.md)을 사용합니다. 검토자가 없으면 self_review라고 표시합니다. 독립 검토·CI 결과를 지어내지 않습니다.

## 현재 검증 명령

```sh
python scripts/check_harness.py
python -m unittest discover -s tests_harness -v
```

제품 pytest/Ruff/설치 스모크는 TASK-001 이후 추가합니다. 새 의존성은 목적·버전 범위·지원 환경·사용 조건을 기록합니다. 최신 버전이라는 이유만으로 무검증 업데이트하지 않습니다.

## 세션 종료

완료/미완료, 변경 파일, 실제 실행 명령, 실패/제약, 다음 단일 작업을 [세션 템플릿](harness/templates/session.md)에 기록합니다. state.json의 active_task, next_task, last_session과 원장을 갱신합니다. 테스트를 실행할 수 없는 환경에서는 그 한계를 명시합니다.

소유자의 명시 승인 없이 강제 push, 보호 설정 변경, 라이선스 선택, 릴리스 게시, 자동 병합·배포를 하지 않습니다. 작업 브랜치에서 PR로 전달하는 것을 기본으로 합니다.
