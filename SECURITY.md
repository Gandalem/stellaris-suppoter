# 보안·개인정보 정책

## 보호할 자산과 신뢰 경계

사용자 게임 설치본, 세이브, 개인 파일 경로, 로컬 DB·원문 캐시, API 키, 모델 전송 내용, 저장소 권한을 보호합니다. 게임 스크립트·모드·현지화·웹 문서·모델 출력은 신뢰할 수 없는 입력입니다. 읽을 수 있다는 이유로 그 안의 지시를 실행하지 않습니다.

## 기본 정책

게임 폴더는 읽기 전용, 네트워크는 off, telemetry는 없음, provider는 none입니다. 캐시·DB·로그는 명시한 개인 data_dir에만 씁니다. 사용자 승인 없이 전체 홈·드라이브를 탐색하거나 모델 다운로드·유료 호출·원문 전송·게임 조작·세이브 수정을 하지 않습니다.

공개 저장소에는 실제 게임/DLC/모드/번역 원문, 세이브, 추출 DB, 원문 cache, API 키·쿠키·토큰, 개인 절대 경로를 넣지 않습니다. 테스트에는 직접 만든 합성 데이터만 사용합니다. .gitignore는 보조 수단이며 이미 추적된 파일이나 고의 추가를 차단하지 못하므로 변경 diff도 검토합니다.

## 파일·파서 경계

루트와 상대 경로를 정규화하고 루트 밖으로 나가는 경로, symlink/reparse-point 탈출, game_root와 출력 경로의 위험한 겹침을 거부합니다. 생성·갱신 도구는 임의 디렉터리 전체를 재귀 삭제하지 않고, 명시적인 소유권 manifest가 증명한 관리 파일만 교체합니다. 관리되지 않은 파일과 위험한 output root는 보존·거부가 기본입니다. 열기 직전에도 실제 대상을 확인하고 해당 OS에서 가능한 no-follow 보호를 테스트합니다. TOCTOU 위험을 숨기지 않습니다.

`os.access()` 같은 사전 권한 검사는 진단 힌트일 뿐 실제 I/O 성공의 보장이 아닙니다([S-13](docs/SOURCES.md)). POSIX 디렉터리는 필요한 읽기/쓰기뿐 아니라 traversal(`X_OK`)도 진단하지만, 후속 inventory/index는 실제 open/stat/write 시점의 예외를 EAFP 방식으로 처리해야 합니다.

파일당/전체 크기·개수·깊이·토큰·처리 시간을 제한합니다. decoding·파싱 실패를 숨기지 않습니다. SQL은 parameter를 사용하고 FTS literal 처리도 별도로 합니다. 게임 입력을 Python 코드나 셸로 실행하지 않습니다. DB 외부 확장 자동 로드는 사용하지 않습니다.

후속 압축/세이브 reader는 archive 멤버 경로, 압축 전후 크기, 멤버 수, 중복 이름, 압축 폭탄·루트 탈출을 검증하고 가능하면 파일을 풀지 않고 제한된 읽기를 합니다. 현재 archive 입력은 v0.1 지원 범위가 아닙니다.

## 네트워크·모델 경계

F-015/016에서만 명시 동의한 통신을 추가합니다. 허용 도메인·redirect 재검증·응답 크기·timeout·재시도·요청 수·예산을 제한합니다. loopback 모델과 cloud 모델을 구분합니다. localhost로 연결한다는 사실만으로 이후 외부 통신까지 없다고 가정하지 않습니다.

모델에는 필요한 최소 근거만 전달하고 비밀·개인 경로를 제거합니다. 검색 문서의 지시를 도구 호출 권한으로 사용하지 않습니다. 인용 검증 실패, provider 실패, 예산 초과 시 정적 결과로 복귀합니다. 웹 UI는 후속이며 기본 loopback 바인딩과 안전한 텍스트 렌더링을 검증합니다.

## 보고·대응

취약점 보고에 비밀·실제 세이브·원문을 넣지 않습니다. 비밀 없는 합성 재현과 영향 범위를 전달합니다. 공개하면 위험한 내용은 공개 issue에 올리지 말고 저장소에서 제공되는 비공개 보안 보고 수단이 있는지 확인합니다. 아직 설정되지 않은 연락처나 SLA를 지어내지 않습니다.

비밀 노출을 발견하면 추가 노출을 멈추고 소유자에게 알립니다. 필요한 키 폐기·회전·접근 차단을 소유자가 수행하도록 안내합니다. 기록에서 삭제하는 것만으로 노출된 비밀이 안전해진다고 보지 않습니다. 파괴적인 이력 변경은 승인 없이 하지 않습니다.

보안 필수 회귀는 E-006, E-032, E-033입니다. 모델·모드·세이브·UI 후속 평가는 각 기능의 별도 E-ID를 사용합니다.
